# relationships user context (TEMPLATE)

_Generated by `/setup-relationships`. See that command's spec for the peer-import + native-question logic that fills this file._

_Identity, voice, and CRM properties are read live at runtime from peer-plugin files (`identity.md`, `voice.md`, `ops.user-context.md`). ICP, Apollo/signal preferences, and referral cooling rules are NATIVE to this plugin as of the 2026-09-15 lead-engine/referral-engine merge — captured directly by `/setup-relationships` and stored below, not read from a peer file._

_Last updated: [filled by setup]_

---

## Identity (minimal — for template variable filling)

- **Name:** [from `identity.md`]
- **First-name form:** [used in templates, e.g., "Zach" vs "Zachary"]
- **Company:** [from `identity.md`]
- **One-liner:** [one-sentence description; from `identity.md`]

## Companion plugins (runtime-detected)

- **cortex:** [Y/N — `<config-root>/memory/` exists]
- **ops:** [Y/N — `ops.user-context.md` exists; if Y, CRM config comes from here]
- **briefing:** [Y/N]
- **comms:** [Y/N — if Y, voice rules come from here in addition to `voice.md`]

## ICP & signal sourcing (native — absorbed from lead-engine, 2026-09-15)

- **Target roles:** [comma-separated titles]
- **Industries:** [comma-separated, or "horizontal"]
- **Company size:** [range]
- **Disqualifiers:** [list, or "none configured"]
- **High-priority signals:** [comma-separated from the 7-signal taxonomy — see `references/seven-signals.md`]
- **Custom signal(s):** [free-form description, or "none"]
- **Apollo:** [enabled? — defaults to job-changes-only / job-changes+funding / all three]
- **Daily DM send budget:** [number, default 10-15]
- **Follow-up cadence:** [3-7-14 / 5-10 / 7-14 / custom]

## Referral network (native — absorbed from referral-engine, 2026-09-15)

- **Connector taxonomy:** [CRM property / list membership / tag / past-customers-auto]
- **Quiet threshold:** [days, default 60]
- **Trigger patterns:** [which defaults apply + any custom]
- **Ask cadence cap:** [days, default 180]

## Tiers (configurable, defaults shown)

- **Inner Core:** size [10–15] · cadence [14] days
- **Strategic:** size [30–50] · cadence [30] days
- **Operational:** size [100–200] · cadence [90] days
- **Dormant:** trigger-only

## Close personal track

- **Enabled:** [Y/N — default N]
- **Size:** [if Y, e.g., 10]
- **Cadence:** [if Y, days; default 30]

## Buckets

- **New Business Development:** weight [1.0]
- **Relationship Building:** weight [1.0]
- **Network Expansion:** weight [1.0]
- **Hide empty buckets:** [Y/N — default N]

## Network-expansion voices

If the user posts in more than one voice (e.g., personal brand + business brand), name each and what surface it's used for. Each voice block is the surface routing target for content-posting templates.

```
### Voice: [name — e.g., "personal" or "brightway"]
- **Surface:** [where this voice is used — LinkedIn personal / business page / etc.]
- **Topic stream:** [practical operational AI / targeted education / etc.]
```

If only one voice is used (the default), just note: `Single voice — uses primary from voice.md`.

## Time budget

- **Typical daily availability:** [5 / 15 / 30 / 60+ minutes — default 30]
- **Highly variable:** [Y/N — if Y, brief asks at top of each run; default N]

## Scoring overrides (optional — advanced)

Defaults in `references/scoring.md`. Override per bucket only if needed.

```
### new_biz bucket
- w_decay: 0.20
- w_icp: 0.40
```

If not specified, all buckets use defaults.

---

## Standalone-install fallbacks

This section ONLY populates when a peer file is missing at setup time (identity/voice/CRM only — ICP/Apollo/referral are always native now, never a "fallback"). The plugin uses these fields directly when the corresponding peer is absent. If the peer is later installed, re-running `/setup-relationships` re-imports and this section gets cleared.

### Identity fallback (only if `identity.md` missing)
[full identity block]

### Voice fallback (only if `voice.md` missing)
[voice descriptors, sign-off, banned phrases]

### CRM fallback (only if `ops.user-context.md` missing)
[CRM tool, owner ID, pipeline stages, custom property names]

---

## Provenance (auto-generated by setup)

Track which fields came from which peer file vs. were answered natively. Helps debugging when something feels off.

```
- Identity: <config-root>/memory/me/identity.md (2026-05-11)
- Primary voice: <config-root>/memory/me/voice.md (2026-05-11)
- CRM: <config-root>/plugins/ops.user-context.md (2026-05-11)
- ICP & signal sourcing: native answers (2026-09-15) — migrated from legacy lead-engine.user-context.md if present
- Referral network: native answers (2026-09-15) — migrated from legacy referral-engine.user-context.md if present
```
